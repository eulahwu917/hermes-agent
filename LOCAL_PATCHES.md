# Locally-carried patches on `main`

This file tracks every commit we carry locally on top of `origin/main` that
isn't (yet) merged upstream. **Check this file — and re-verify each entry's
merge status — before running `hermes update`.** A plain `git pull --ff-only`
will fail cleanly if any of these are still unmerged (it won't silently drop
them), but the update won't proceed until you rebase, so know what you're
carrying before you start.

## Update procedure when entries exist below

1. Read this file. For each entry, run `gh pr view <PR#> --repo NousResearch/hermes-agent --json state,mergedAt`.
2. **If merged:** drop the local commit — `git rebase` will skip/fail-empty on it
   once the equivalent content is in `origin/main`; remove its row from this file.
3. **If still open:** `git fetch origin main && git rebase origin/main` — this
   replays the commit on top of the new tip. Re-run whatever test suite the
   commit's own description calls for (see "Verify" column) before trusting the
   rebase succeeded silently — a clean rebase doesn't guarantee the patch's tests
   still pass against new upstream code.
4. Update this file's "Last verified" date after any check, even if nothing changed.

## Currently carried

| Commit (short) | What it does | Upstream PR | PR status (last checked) | Verify before trusting | Last verified |
|---|---|---|---|---|---|
| `22251b9dc3` (branch `wt/t_35758409`, fork eulahwu917) | **Kanban assignee-resolution fail-fast (card t_35758409).** A card assigned to a profile that does not exist on disk sat in ready/todo forever with zero diagnostic (2026-09-14: 10 cards, the ready ones parked 28–78 min). `kanban_db.assignee_resolves_to_profile()` (the dispatcher's own `profile_exists` primitive) backs: creation/assignment-time `assignee_unresolved` events on the card (every surface — CLI, `kanban_create` tool, dashboard, API); loud stderr warnings on CLI create/assign/reassign + a `warnings` list on the kanban_create tool result; dispatch-time surfacing (`DispatchResult.assignee_unresolved`, one deduped per-card dispatch-stage event, a log warning, a `!!` line in `hermes kanban dispatch`). Never fails open (no guessed reassignment); deliberate external lanes (terminal `claim_task` pullers) are untouched. Weekly Tier-1 enumeration (ready/todo orphans with card ids) ships separately as a patch to `~/.hermes/scripts/weekly-infra-health-tier1.py` — installed by Bepop (default profile) per the t_35758409 handoff. | Local custom patch (upstream candidate — lane-compatible by design; upstream PR routing is Bepop's call) | N/A — local only | `HERMES_PYTHON=/home/david-wu/.hermes/hermes-agent/venv/bin/python scripts/run_tests.sh tests/hermes_cli/test_kanban_assignee_resolution.py` → 11/11; adjacent suites (test_kanban_cli, test_kanban_db, test_kanban_host_cap, test_kanban_diagnostics, test_kanban_tools, test_kanban_provenance) → 97/97 via run_tests.sh; full `-k kanban` batch parity with unpatched main (same 14 pre-existing env/ordering failures both sides, none attributable to this patch) | 2026-09-14 |
| `8b8aa83726` (2026-09-06 rebase of `dabfce2275` onto `origin/main`@`693641aa8b`) + `e187c1bbc5` (pyproject.toml pin bump) | Hindsight plugin: `recall_min_scores` relevance floor + `prefer_observations` flag (dormant). Combines our PR #71122 with `prefer_observations` cherry-picked from #64914. Full design history: `/srv/personal/vault/90-system/2026-07-25--hindsight-client-upgrade-runbook.md`. **2026-09-06 re-home (0.21.0):** upstream moved constants/normalizers into `plugins/memory/hindsight/settings.py` and compacted the tool handlers into `_tool_*` + a generic dispatch — `_normalize_min_scores`/`_VALID_MIN_SCORE_KEYS` now live in `settings.py`; recall wiring sits in `_recall()` (single arecall site); thread-routing override re-homed to the end of `_apply_recall_settings()`. | [#71122](https://github.com/NousResearch/hermes-agent/pull/71122) (ours) + [#64914](https://github.com/NousResearch/hermes-agent/pull/64914) (prefer_observations only) | OPEN (re-verified 2026-09-06) — teknium1 left changes-requested review (version pin leak, non-finite float acceptance, stale README). Comment posted acknowledging all 3, supporting consolidation on #64914 as canonical. All three fixes already applied locally. | `pytest tests/plugins/memory/test_hindsight_provider.py` — **107/107 passing (2026-09-06, post-0.21.0 rebase**; count dropped because upstream split/rewrote many tests, not coverage loss). Test imports re-homed to the settings module. | 2026-09-06 (rebase + full suite); gateway restart + live recall verified 2026-07-26; PR status re-verified 2026-09-06. **Gateway restart CONFIRMED LIVE 2026-09-06 18:04 PDT** (PID 43172, clean startup, live recall verified through the restarted process). |
| `53803419ba` | Hindsight plugin: error formatting fix — 3 tool error handlers now include `{type(e).__name__}: {e}` instead of bare `{e}` (which produced blank messages for TimeoutError). Retain handler adds TimeoutError-specific guidance to verify via recall before retrying. `_DEFAULT_TIMEOUT` comment updated to reference config.json override path. Also: `"timeout": 300` set in `~/.hermes/hindsight/config.json` (profile-scoped, no code change). **2026-09-06 re-home (0.21.0):** the three tool error handlers collapsed into the generic `handle_tool_call` dispatch — the `type(e).__name__` prefix + TimeoutError retain-guidance now live there (one site covers all three tools); the `_DEFAULT_TIMEOUT` comment moved to `settings.py`. Full design history: `/srv/personal/vault/90-system/runbooks/2026-08-17--vault-hindsight-improvement-runbook.md` | Local custom patch (not for upstream) | N/A — local only | `py_compile plugins/memory/hindsight/__init__.py` — passes. `grep -n "type(e).__name__" hermes_cli/../plugins/memory/hindsight/__init__.py` — 1 match (generic dispatch). Suite green. | 2026-09-06 (rebase); original verification 2026-08-18. |
| UPSTREAM CANDIDATE → **CARRIED 2026-08-27**: PR [#95984](https://github.com/NousResearch/hermes-agent/pull/95984) (ours) fixes #87503 — Codex OAuth singleton root write-through + root-resolved direct-write + reuse-rescue (C1/C2/C3), closing the weekly multi-profile refresh_token_reused family-death cycle diagnosed 2026-08-26/27. Carried as THREE commits (`768ce70431` + `955df99641` + `2282d7f634` = 2026-09-06 rebase of the f9fb9b812+9bedf6211+918590a3c chain onto `origin/main`@`693641aa8b`) on local main; **2026-09-06 re-home (0.21.0): upstream split auth.py (~9.5k lines) into auth_codex.py/auth_constants.py/etc. — the implementation now lives in `hermes_cli/auth_codex.py` with late-bound `from hermes_cli.auth import ...` inside each function (module convention, so `monkeypatch.setattr(A, ...)` still intercepts); `hermes_cli/auth.py` gained the re-export lines; T18's structural pin + test file re-homed to `auth_codex.py`**; 27/27 module tests + full codex/auth sweep green on live checkout (with `HERMES_T18_EXTRA_NONPROD` env listing sibling-patch files for T18's budget pin). Built from spec v11 (11 Reviewer rounds, APPROVED t_1d6b461e); source branch preserved in `/srv/repos/hermes-agent-codexoauth`. **GATEWAY RESTART: CONFIRMED LIVE 2026-08-30** — restart happened 2026-08-27 20:23 (after the Aug 26 patch commits landed on local main); process ancestry + zero `refresh_token_reused` errors in all gateway logs since Aug 27 verified. Earlier "restart still pending" note was stale (missed the Aug 27 restart). Upstream merge will retire this row per standard procedure. | [#95984](https://github.com/NousResearch/hermes-agent/pull/95984) | OPEN (re-verified 2026-09-06; PR includes remediation + test-hardening commits) | `pytest tests/agent/test_codex_singleton_write_through.py` — **27/27 (2026-09-06)** w/ HERMES_T18_EXTRA_NONPROD set to the full carried-file list; `pytest tests/agent/ -k "codex or auth"` — 721 passed / 2 skipped (2 optional-SDK failures fixed by installing pinned `anthropic==0.87.0` extra; then 54/54). | 2026-09-06 |
| `303550f5dc` (2026-09-06 rebase of `f0a0e67281`) | T19 ghost-cwd local-backend guard: `_resolve_command_cwd()` ignores a recorded session cwd whose directory no longer exists (env_type == "local" only) and falls back to default_cwd — sessions self-heal instead of dying with exit 126 on every bare terminal call after their Kanban worktree is deleted. Container backends stay under `_is_unusable_container_cwd`; remote backends deliberately not probed locally. +6 tests (`test_ghost_cwd_fallback.py`), `test_terminal_task_cwd.py` fixture made physically real. Diagnosed 2026-09-01 (6+ affected sessions Aug 31–Sep 1, 3 deleted worktrees); Reviewer APPROVED t_446a36c8 (mutation A+B probes corrected per review); upstream comment posted on [#62169](https://github.com/NousResearch/hermes-agent/issues/62169#issuecomment-5503371494) (complementary to stalled #62189). | [#100823](https://github.com/NousResearch/hermes-agent/pull/100823) (ours) | OPEN (opened 2026-09-01, awaiting upstream review) | `HERMES_PYTHON=venv/bin/python scripts/run_tests.sh tests/tools/test_ghost_cwd_fallback.py tests/tools/test_terminal_task_cwd.py tests/tools/test_session_cwd_store.py tests/tools/test_container_cwd_sanitize.py tests/tools/test_interrupted_command_cwd.py -q` → **44/44 (2026-09-06**; was 42/42 — upstream added 2 cases to the same files); `grep -c 'and env_type == "local"' tools/terminal_tool.py` → 1 | 2026-09-06 (rebase); original diagnosis/verification 2026-09-01. **GATEWAY RESTART: CONFIRMED LIVE 2026-09-06 18:04 PDT** (gateway restarted onto v0.21.0 with this fix carried; PID 43172, clean startup logs, zero errors). The interim workaround note is retired. |

| single squashed commit (this commit — see `git log` on local main; the row lives inside the feature commit itself) | **Cron Attention (Discord-exit Phase 2, spec v1.4 §3 — APPROVED t_f78690b9, built on t_fb3a4b93).** Unified Attention read model: new `cron/attention.py` (union of `cron_incidents` + `attention_events` [table created, Phase-3 receiver writes it later], `list_attention(open_only)` + `ack_attention(kind,id)` with closed-ok/already-closed/unknown-id outcomes, strict filter validation, `cron/attention.changed` sentinel on every ack commit); `cron.incidents` episodic re-arm (a closed signature mints a NEW episode row on recurrence — permanent-suppression pin deliberately changed) + sentinel touch on CLI/RPC ack; `cron.scheduler` WARNING raise on incident-write failure + per-job `alarm` flag (no_agent stdout = incident regardless of exit code; `cron/jobs.py` field); `tui_gateway/methods_tools.py` cron.manage gains `list_attention`/`ack_attention` actions; `tui_gateway/change_watcher.py` gains `attention.changed`. Desktop: Attention section on the Scheduled-jobs page (rows: name/sig/timestamps/excerpt, [Ack] with failed-ack-row-stays + error toast, [View output] into the file rail), sidebar Scheduled-jobs nav badge from the LIVE list with a stale marker on failure, live refresh via attention.changed + 30s backstop + focus refresh, i18n en/zh. Review rounds 1–3 hardening folded in: SILENT_MARKER sentinel excluded from the alarm gate + regression control; disconnect/failed-ack drive the stale state (retained rows, generation-invalidated late reads); attention cache reset/reseed + ack scope token on profile/connection switches (gateway-switch wipe + effect scope deps); body_excerpt sourced from the OUTPUT FILE with honest error-text fallback; oracles repaired (case 10 ≤ one backstop period, case 11 attention-specific stale states with no synthetic refreshes, E1 matrix over the live serve WS [`/api/ws` + handle_ws + token auth], `zz-test-` prefix on fixture job ids); round 4: ack scope pinned through the real requester (use-gateway-request `scopeGuard` checked before the send AND before every reconnect/retry — A's ack can never replay on B), obsolete-scope list/ack results side-effect-free (ownership checked before payload validation/atom writes; `AttentionScopeChangedError` surfaces as a non-event in CronView), E1 matrix runs cases 1–7+14 over the live WS fixture + a one-connection cases 1–4 matrix (normalized post-ack read at that boundary, B unchanged, unknown-id zero mutation, idempotent timestamps), e2e case 8 asserts the exact preview tab + a beyond-excerpt file-only marker (a no-op View-output handler fails). Tests: §3.4 14-case contract (`tests/cron/test_attention.py`, E1/E3/E4 + auth fail-closed + negative control + CLI parity + R2-3a scheduler-loss test; production 576b65cc2144 row snapshotted read-only before/after) + `e2e/cron-attention.spec.ts` (cases 8–11) + vitest store/action tests. Review round 4 hardening folded in: `requestGateway` revalidates the scope guard AFTER the asynchronous reconnect and immediately before the recovered replay (`scope changed before replay`), and an obsolete-scope recovery bails before publishing its descriptor or dialing the shared primary object — a Settings connection apply landing while the reconnect is mid-IPC can no longer replay A's ack on B (R4-1). Regression control parks the real primary recovery in `getConnection()` past the first guard, applies the real Attention scope teardown + B's descriptor publication, and asserts zero replay/publish/dial with B's Attention live and its row unclosed (red on base: the recovered send replayed and clobbered B's connection). Round-5 hardening folded in: obsolete-scope recovery FAILURE is side-effect-free too — the shared catch bails before `setConnection(null)` or any reauth-error stash when the scope guard reports torn down (getConnection / ticket-mint / dial rejections alike), preserving same-scope cleanup; regression: parameterized obsolete-scope rejection at each recovery await (B's descriptor identical, exactly one send, no post-failure dial, B's Attention live/current) + a same-scope reauth-failure control that still clears the stale descriptor. Verify: 98-file Python run 1323 passed / 0 failed / 2 skipped (R4 reviewer receipt) + attention/incidents 28/28 this round (production-row protection active, HOME=/home/david-wu); desktop typecheck (3 tsconfigs) + build + focused vitest 81/81 + full vitest 9458 passed / 1 pre-existing env failure (electron ssh-connection sun_path) / 6 skipped + e2e 4/4 (xvfb, twice consecutively). Round-6 hardening folded in: (R6-1 MEDIUM) the Attention READ now pins its scope through the real requester exactly like the ack — `refreshAttention` passes the scopeGuard as the request's 5th-arg options, so an obsolete scope's read recovery can no longer publish its stale descriptor over the new primary (fulfillment) or clear B's published descriptor in the recovery catch (rejection); (R6-2 HIGH) the background Attention refresh reads through the raw AMBIENT requester (new required `attentionRequestGateway` prop on useBackgroundSync, wired from `ambientRequestGateway` in contrib/wiring.tsx) instead of the focused-session dispatcher — a focused other-profile Bot tile can no longer receive A's poll, nor replace A's badge/rows with its own (possibly empty) snapshot; transcript/session requests stay on the owner dispatcher (routing semantics untouched). Controls: `use-gateway-request.test.ts` parameterized read-recovery test through PRODUCTION `refreshAttention` + real requester (parked `getConnection`, real `resetAttention`+B-rows+B-descriptor publication, fulfill + reject arms — asserts exactly one send, no obsolete publish/dial/re-mint, B's descriptor identical, B's Attention live/current; RED on base: fulfills → recovered read replayed [2 sends]; rejects → catch `setConnection(null)` erased B's descriptor; no guard on the read); `use-background-sync.test.tsx` wiring-level control (ambient A nonempty + focused B empty, parked A read spans focus changes and an attention.changed tick rerun — poll/event reads stay ambient [3 ambient cron.manage reads, dispatcher never carries one], guard options end-to-end through the ambient requester, A's rows never replaced; RED on base: dispatcher's empty snapshot committed live and the ambient requester never called; the two lifecycle tests also red pre-fix). Verify (this round): HOME=/home/david-wu HERMES_PYTHON=<main-venv> scripts/run_tests.sh tests/cron/ → 95 files / 1232 passed / 0 failed / 2 skipped (production-row protection active); desktop typecheck (3 tsconfigs) exit 0; build + assert-dist-built exit 0; vitest focused 18 files / 229 passed (src/app/{cron,gateway/hooks,contrib}) + 5 core files 77/77; eslint on all 7 changed files 0 errors / 0 warnings; e2e 4/4 (xvfb). Round-7 hardening folded in: (R7-1 MEDIUM) cron.manage `list_attention` now validates the RAW supplied filter values BEFORE any lossy coercion — `open_only` must be a real bool when supplied (omitted/null keeps the documented default True; an unknown string or any object can no longer silently coerce to False via is_truthy_value) and `state` must be a wire-state string when supplied (omitted/null = no filter; falsey non-strings false/0/''/[]/{} are explicit 4066 'invalid attention filter/state filter' errors, never silently None via _str_arg → an empty-list success); `cron/attention.list_attention` gained the matching open_only type guard for direct callers. Controls: parameterized live-WS negatives (tests/cron/test_attention.py, +10 cases) for invalid open_only ('bogus',1,0,[],{}) and falsey/non-string state (false,0,'',[],{}) — empty-DB control errors instead of empty-list success, seeded control errors with unchanged-row raw-E4 read-back, valid filters still work after; RED on base: all 10 failed at the pre-fix silent-coercion success envelopes. Verify (round 7): HOME=/home/david-wu HERMES_PYTHON=<main-venv> scripts/run_tests.sh tests/cron/ tests/tui_gateway/test_change_watcher.py tests/test_cron_manage_profile_scope.py tests/tools/test_cronjob_tools.py → 98 files / 1333 passed / 0 failed / 2 skipped (production-row protection active; attention+incidents 38/38); desktop typecheck (3 tsconfigs) exit 0; build + assert-dist-built exit 0; focused vitest 5 files / 77 passed; e2e 4/4 (xvfb; case10 939ms ≤ 30s). | Local custom patch (not for upstream) | N/A — local only | `scripts/run_tests.sh tests/cron/` + desktop `npm run typecheck` + `npx vitest run` + `npx playwright test e2e/cron-attention.spec.ts` (xvfb-run) | 2026-09-08 |
| `7271994c86` (base `7fc2e1b24d`) | **Desktop workstream-scoped recall (SPEC-INFRA-DESKRECALL-001 v4, built t_697ff894).** Hindsight plugin gains `_effective_recall_filter()` resolved per recall call inside `_recall()`/`_reflect()` (every entry point incl. tools + sync prefetch): for `platform == "desktop"` only (no platform:thread override applied, and `recall_sync == true` — otherwise the branch trips closed and logs), the session cwd (session ContextVar override OR terminal-scope fallback via `agent.runtime_cwd.resolve_context_cwd()`) resolving under config.json `desktop_context_root` selects the bare domain key in `thread_routing.json` (loader: colon-less keys are domain keys, same shape/validation/dormant semantics) and returns its nonblank `extra_tags` as `any_strict`; every other case fail-opens to the global baseline (never replaced with an empty filter). New test file `tests/plugins/memory/test_hindsight_desktop_recall.py` (31 tests: outgoing-filter/tool provenance, interleaved two-session isolation via the real ContextVar setter, full fail-open matrix incl. nonexistent-override finality + symlink boundary + malformed/missing/non-object table + dormant routes, Discord regression controls, sync-prefetch injection + log-envelope failure signal, sync-mode guard). | Local custom patch (data application — 12 routing keys, 12 pack sections, config.json `desktop_context_root` — plus gateway restart and live probe owned by Bepop after diff review) | N/A — local only | `HERMES_PYTHON=/home/david-wu/.hermes/hermes-agent/venv/bin/python scripts/run_tests.sh tests/plugins/memory/test_hindsight_desktop_recall.py` → 31/31; hindsight sweep (8 files) 173 passed / 0 failed | 2026-09-09 |
| `36cb8f1e1b` (branch `feat/hindsight-desktop-recall-scoping`, fork eulahwu917; 3 commits over upstream base `6c3d4a4af7`) | **Desktop-scoped recall — UPSTREAM EXTRACTION of the `7271994c86` row above** (PR [#107802](https://github.com/NousResearch/hermes-agent/pull/107802), OPEN). Extraction review chain R1→R4 (t_26d17b7c, t_3aca815d, t_d253e269, t_795e9940) — R4 APPROVED, binds head `36cb8f1e1b` + PR body sha256 `0a2b449f…57edb`; David posting approval 2026-09-10 (items 1-4, chat). Diff: plugin +220/−5, new desktop test file +857, README +45; LOCAL_PATCHES.md deliberately absent from the branch. | Local custom patch | [#107802](https://github.com/NousResearch/hermes-agent/pull/107802) — OPEN (posted 2026-09-10); subscribed; changes-requested → same-branch fix cards per the runbook pattern | Rebase/retirement stop-condition: `git log --oneline 6c3d4a4af7..origin/feat/hindsight-desktop-recall-scoping` vs the squash that actually lands upstream — row retires when the landed content supersedes the branch; also re-check `git ls-remote upstream main` vs `6c3d4a4af7` at each rebase (if upstream gains commits to `plugins/memory/hindsight/__init__.py` / `agent/runtime_cwd.py`, re-measure conflict shape before rebasing) | 2026-09-10 |

| `2d39cc906e` (branch `wt/t_62cf0f31`, fork eulahwu917) | **Kanban worker exit-guard run-identity fix (card t_62cf0f31, evidence t_f158f2e6).** The turn-end stop nudge no longer fires at an implementation run that already reached a terminal outcome (``review_requested`` handoff) or whose card is now owned by a different run (a review-claim transition, not stale work). ``agent/kanban_stop.py`` gains ``worker_run_finished()`` (env-pinned HERMES_KANBAN_RUN_ID vs ``tasks.current_run_id`` + terminal-outcome check; missing env / unreadable board fail closed to the legacy nudge); ``hermes_cli/kanban_db.py`` gains ``read_worker_run_state()`` (``mode=ro`` peek, NEVER creates/initializes the board, ``(None, None)`` on any failure). Also fixes the misdirection where the nudge told a post-handoff implementer to call ``kanban_complete``/``kanban_block``, which the board kernel rejects after ``review_requested``. | Local custom patch (not for upstream) | N/A — local only | `pytest tests/agent/test_kanban_stop.py` — **9/9 (2026-09-11**, incl. an end-to-end replay of the t_f158f2e6 timeline); adjacent suites via scripts/run_tests.sh — 112/112 (7 kanban/stop-gate files 109/109 + test_verification_stop_caching 3/3); ruff clean on all 3 changed files | 2026-09-11 |

## 2026-09-06 rebase notes (0.20.5 → 0.21.0, +7,564 upstream commits)

- **Architecture changes that forced re-homes:** hindsight constants/normalizers →
  `plugins/memory/hindsight/settings.py`; tool handlers → compact `_tool_*` methods +
  generic `handle_tool_call` dispatch; single `_recall()`/`_reflect()` helpers (one arecall
  site, not two); `hermes_cli/auth.py` (~9.5k lines) → split into `auth_codex.py`,
  `auth_constants.py`, `auth_xai.py`, etc., with `auth.py` keeping lazy re-exports.
- **Updater trap unchanged:** `update_cmd.py` still executes `git reset --hard origin/main`
  when the checkout is on the update-target branch and ff-only fails (same-branch divergence
  = our permanent state). Manual rebase per this file's procedure remains REQUIRED; never
  run bare `hermes update` while carrying commits.
- All four upstream PRs re-verified OPEN 2026-09-06 via `gh pr view`.
- Pre-update safety artifacts: branch `backup/pre-update-20260906` +
  `/srv/personal/scratch/hermes-agent-pre-update-20260906.bundle` (534MB full bundle).
- Config migrated 38 → 41 (`hermes config migrate`); update-check caches cleared.

## 2026-09-13 re-home notes (recovery from the accidental v0.21.2 update)

Four semantic defects were re-verified after the replay; these notes record the mechanics for the next re-home:

- **Attention ledger plumbing (`cron/attention.py`):** v0.21.2 deleted `ledger_transaction`/`open_ledger`/`prepare_ledger` from `cron.executions` (the shared SQLite stack moved to `hermes_cli.sqlite_util`). Re-homed exactly like `cron.incidents`: `open_db(..., initialize=_initialize_schema)` + `transaction()` under the module lock; the schema hook now delegates to `cron.executions._initialize_schema` + `cron.incidents._initialize_schema` (both idempotent) before creating the attention tables.
- **Hindsight pins (F1 fix, `8a9486df4d`):** `tools/lazy_deps.py` / `pyproject.toml` now widen `hindsight-client` to a tolerant ranged floor `>=0.6.1,<1` instead of the exact `==0.6.1` reverted during the 2026-09-13 re-home. An exact pin cannot stand with the carried recall floor: with lazy installs disabled, `ensure()` hard-raised (`FeatureUnavailable` → `ImportError`) at client construction on a supported host; with installs enabled, it re-downgraded an installed 0.8.5 back to 0.6.1 — undoing `_maybe_upgrade_client()` and silently inerting the floor. The ranged floor admits every supported 0.x (`legacy` 0.6.1 constructs with the floor cleanly disabled via the C4 `>=0.8.4` guard; 0.8.x constructs with the floor enabled) and makes `ensure()` a no-op on an already-installed client, so there is no upgrade-then-downgrade sequence. Feature availability stays gated by `_MIN_CLIENT_VERSION` (0.8.5, best-effort `_maybe_upgrade_client()`) plus the C4 recall-param guard — NOT by the acquisition pin. `plugin.yaml` was already `>=0.6.1` (unchanged). Reconciliation test: `TestClientAcquisitionPolicyMatrix` in `tests/plugins/memory/test_hindsight_provider.py` drives the real `ensure()` path with no preseeded client.
- **min_scores wiring tests:** `test_prefetch_passes_min_scores_when_configured` and `test_hindsight_recall_tool_passes_min_scores` now pin `importlib.metadata.version` to 0.8.5 (the same pattern the prefer_observations tests already used) so they assert the config wiring rather than the ambient venv's `hindsight-client`.
- **T18 file-set pin retired:** the whole-branch non-production file-set assertion is a change-detector on this multi-patch carried branch (upstream AGENTS.md ban). The §SB symbol enumeration on `hermes_cli/auth_codex.py` (added/removed/changed-bodies) is unchanged; the `HERMES_T18_EXTRA_NONPROD` runtime declaration is no longer needed for T18.

## Retired (for history — merged upstream, no longer carried)

_(none yet)_

## Note on the shallow-clone artifact — RESOLVED 2026-07-26

Our local checkout of `~/.hermes/hermes-agent` **was** a shallow clone (repeated
`--depth 1` fetches had left `.git/shallow` with multiple synthetic boundary commits,
including `4281151ae`). This caused `git merge-base main origin/main` to return nothing
and made a routine 2-file rebase surface as hundreds of add/add conflicts across the
entire tree. Fixed via `git fetch --unshallow origin` (2026-07-26) — `merge-base` now
correctly resolves to `4281151ae` as expected. If a future `merge-base`/`is-ancestor`
check against `origin/main` fails unexpectedly again, check `git rev-parse
--is-shallow-repository` first before assuming real history divergence — nothing
currently re-shallows this clone, but a `--depth` fetch by a future script or fresh
clone could reintroduce this.
